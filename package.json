{
  "name": "bun-ffi",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun build-rs && bun start",
    "start": "bun run ./src/index.ts",
    "build-rs": "cd ./rust && cargo build --release"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "ffi-napi": "^4.0.3"
  }
}